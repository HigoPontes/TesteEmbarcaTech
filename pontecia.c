#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a, b, pow = 1;
    printf("Digite o primeiro número: ");
    scanf("%d", &a);
    printf("Digite o segundo número: ");
    scanf("%d", &b);

    for (size_t i = 0; i = b; i++)
    {
        pow = pow * a;
    }

    printf("%d", a);
}